{% extends 'base.html.twig' %}

{% block title %}Couleur{% endblock %}

{% block body %}
    <h1>Couleur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ couleur.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ couleur.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ couleur.description }}</td>
            </tr>
            <tr>
                <th>Active</th>
                <td>{{ couleur.active ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('couleur_index') }}">back to list</a>

    <a href="{{ path('couleur_edit', {'id': couleur.id}) }}">edit</a>

    {{ include('couleur/_delete_form.html.twig') }}
{% endblock %}
